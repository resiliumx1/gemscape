

## Install Framer Motion

Framer Motion is not currently in the project. I'll add it to `package.json` dependencies.

**Change:** Add `"framer-motion": "^11.15.0"` to the `dependencies` in `package.json`.

That's it — one line addition. You can then import `motion`, `AnimatePresence`, etc. from `"framer-motion"` anywhere in the project.

